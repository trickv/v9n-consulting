# Block all web access — this directory contains repo tooling, not public content.
# Works on Apache 2.4+ and 2.2.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
