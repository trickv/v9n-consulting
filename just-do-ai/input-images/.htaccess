# Block all web access — this directory contains source photos and a
# Python virtualenv used for HEIC conversion. Not public content.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
